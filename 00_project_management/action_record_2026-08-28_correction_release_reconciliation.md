# 更正审阅包整合、可移植性核验与 GitHub 同步行动记录

日期：2026-08-28。工作路径：`H:\cuhk-2025fALL\6013RP-wyf`。

本记录接续 `action_record_2026-08-28_post_gateC9_advisor_correction_audit.md`。
前一记录描述归一化和门控缺陷、正式纠错重跑、全文件清点及图件重绘；
本轮完成其尚未完成的校准算术复核、附件整合、文档与包的一致性核验和代码同步。
两份报告保留各自执行时点，不用本轮状态倒改历史行动记录。

## 1. 结论与真实状态

**更正材料现在可以交给作者和外部方法审阅者复核，但仍不是可直接投稿的最终包。**

| 层面 | 本轮结束状态 | 解释 |
|---|---|---|
| 更正校准 | HOLD | 弹性网 B_ASC precision 0.885210，低于未改动的 0.90 门槛 |
| 更正疾病结局 | 未解锁、未计算 | 不存在需要绕过门控“接着跑完”的疾病分析 |
| R1 端到端身份稳定性 | HOLD 保留 | B_ASC median Jaccard 0.930323 < 0.95 |
| 源标签定义的外部 IFN 主分析 | 保留 | 不等于源标签无关的复现 |
| 审阅包技术完整性 | PASS | 大小、哈希、嵌套清单、文档版本绑定及授权边界检查通过 |
| GitHub 代码与精简成果 | 已同步 | 非强制推送，确认远端提交与本地一致 |
| 作者再次批准、对应新归档和投稿 | 未完成 | 不沿用旧版批准冒充重大更正后的最终批准 |

技术 PASS 不会覆盖科学 HOLD，也不意味着原有 IFN 关联被一个新的阴性疾病检验推翻。
旧 C9 PASS 已被纠错审计撤回其支持性效力；旧记录作为历史证据保留。

## 2. 本轮范围

- 读取现有纠错输出、冻结清单、主文、补充材料、RP、投稿文件及历史统计归档。
- 新实现校准算术核验，整合已有结果，不重新拟合原有疾病主模型。
- 继承上一轮全文件清点结果；本轮针对当前交付物进行完整性和内容一致性检查，
  不宣称再次逐值审查全部原始单细胞矩阵或重跑所有历史分析。
- 用户的外部审计附件作为待核实意见，不把其中的发布建议当作可绕过 HOLD 的授权。
- 没有下载新大数据、启动额外大型计算、移动旧标签、发布新 DOI 或向期刊上传文件。

## 3. 校准的另一实现复核

新增 `audit_tools/phase17_postc9_05_recheck_calibration.py`，没有调用原校准函数，
从冻结的参考 OOF 预测独立实现计数和指标计算。

| 项目 | 复核结果 |
|---|---:|
| 参考 OOF 细胞 | 14,300 |
| B_CONV / B_ASC 参考训练细胞 | 13,000 / 1,300 |
| 供体 | 258 |
| 供体分组 folds | 5，未发现同供体跨 fold |
| confidence / margin 候选行 | 72，全部核对 |
| 外部矩阵 | 56 |
| 外部输入 / QC 后 / B-lineage 候选细胞 | 363,083 / 353,527 / 36,630 |
| 弹性网选择 alpha | 0.0001 |
| 弹性网诊断 confidence 阈值 | 0.95，不能称为合格冻结阈值 |
| 弹性网覆盖率 | 0.941958041958042 |
| 弹性网 B_CONV precision | 0.9964495087110891 |
| 弹性网 B_ASC precision | 0.8852097130242825 |

72 行校准结果在 1e-12 容差下与冻结结果一致。质心 margin 按原预测的 float32
精度比较，避免文本读入后精度变化使边界细胞归属发生伪差异。16 个冻结 C9
输出的大小和哈希通过；原始受保护元数据没有用于本轮重算疾病效应。

**“独立实现”仅指同一审计代理使用另一套算术实现；不是独立人类审稿、
另一支研究团队复现、模型重拟合或无偏外部性能验证。** 同一供体不跨 fold
也不能消除既有特征选择和超参数选择未完整嵌套的限制。

证据保存在 `00_project_management/post_gateC9_release_review_2026-08-28/` 下的
`calibration_recount.csv` 和 `calibration_recount_audit.json`。

## 4. 保护历史成果与新构建入口

发现旧发布入口会重新生成过时的 canonical 文字，并删除或覆盖旧包。当前分支
已在入口处明确阻止执行；旧实现主体和 Git 历史保留用于追溯：

- `audit_tools/build_submission_package.ps1`。
- `audit_tools/run_6013RP_phase17_gateC8BRF_author_release.ps1`。
- `audit_tools/phase17_c8brf_02_build_submission_sources.py`。
- `audit_tools/phase17_c8brf_03_build_documents.py`。

新增 `audit_tools/build_correction_review.ps1` 和对应 Python 构建器。
PowerShell 以脚本自身定位项目根目录，避免从 `C:\Users\Administrator` 运行时
找不到相对路径。构建器只接收新的空目录；已有 ZIP 或非空目录会使其拒绝执行。
它核对冻结结果、源码和 WPS 文档来源，只做材料装配，不解锁科学分析或批准投稿。

`04_submission/journal_submission/` 与其 ZIP 没有覆盖、删除或改写。
本轮第一次生成的审阅包因作者批准文字需要修正，保存在审计目录的
`_build_attempts/initial_bundle/`，不是当前交付物；它和可移植测试展开目录均未进入 Git。
本轮没有开展额外的破坏性清理。

## 5. 当前审阅包与附件守恒

当前目录：`04_submission/correction_review/`。

当前 ZIP：`04_submission/correction_review.zip`。

| 指标 | 结果 |
|---|---:|
| ZIP 大小 | 26,013,157 字节 |
| 外层有效载荷文件 | 68，另加外层清单 |
| 文档 | 4 份，每份 DOCX 和 PDF |
| 主图 / 补图 | 5 / 10，每张 PDF 和 PNG |
| Figure Source Data 清单行 | 15 |
| Full Statistical Results 清单行 | 184 |
| Regulator Sensitivity 清单行 | 10 |
| 草拟上传角色 | 11，全部标为 DRAFT_NOT_FOR_UPLOAD |

ZIP SHA-256：

```text
DA07D1D7F87E559A7778618FDFAE5BD55DA77291E1F0BAA44B915CDE209B5993
```

完整统计附件的 **163 个历史文件逐字节保持不变**。增加
`external_mapping_calibration/` 的 20 个文件，以及 1 份当前范围说明，形成
184 项有效载荷清单。新增 20 文件由以下内容构成：

- 14 份精简纠错运行记录，含参数、完整候选族、样本/聚类清点、决策和原冻结清单。
- 2 份本轮校准算术复核记录。
- 1 份 Figure S10 Source Data。
- 1 份技术纠错契约。
- 1 份明确列出本地保留项的索引。
- 1 份解释范围与推断限制的 README。

参考逐细胞全库审计、参考 OOF 预测和外部逐细胞预测仍留在本地，未装入公开附件。
随包保留它们的大小与哈希；原 C9 清单被明确标为本地运行来源记录，而非当前
精简附件的完整文件清单。当前附件以其外层 manifest 判断完整性。

使用固定条目顺序、时间和 ZIP 元数据构建两次，结果逐字节相同。
这仅证明相同输入字节在本次环境内确定性打包，不代表重新 WPS 导出后
DOCX/PDF 必然字节相同，也不代表跨操作系统数值完全相同。

## 6. 稿件、RP、声明与图件核验

- 主文 Additional file 4 和 Supplementary Table S8 与新增校准附件内容相符。
- RP 保留更正后的证据层级、beta-binomial 方法、身份 HOLD、源标签依赖和非因果边界。
  其“期刊特定投稿包尚待重建”仍成立：本轮是无指定期刊的审阅包。
- 投稿信重新生成，去除固定期刊称谓，压缩到一页，并如实写明更正和待再次批准状态。
- 第一作者 Zhi Chen、通讯作者 Teng Qi、邮箱、ORCID、单位及已记录的声明保留。
- 主文、作者确认文件及投稿信不再把此前的批准视为对重大更正稿的再次最终批准。
  主文明确写出 renewed final approval is pending；AI 使用声明保留作者核实责任。
- 旧 DOI `10.5281/zenodo.22086892` 只表示初始历史快照。没有新 DOI 或新发布已完成的说法。
- `Zenodo_Metadata.json` 保持初始发布元数据原样，README 明确其历史范围，不供新版本直接复用。
- 投稿文件名采用 Manuscript、Supplementary_Information、Figure_1 等中性名称，
  不将内部 Gate、日期、稿件版本号塞入面向期刊的主要文件名。
- 许可证及第三方数据范围说明随包保留，本轮没有修改许可证或扩张原授权范围。

本轮复用前轮从冻结数据生成的 15 张图，不编辑图中数值或重新运行疾病模型。
45 份图件/图源文件的清单全部通过。图形采用既有 170 mm 画布和矢量 PDF / 600 dpi PNG；
这不是声称 170 mm 是 Nature 标准双栏宽度，也不是对未选期刊的格式最终验收。

## 7. WPS、文档绑定与版面

本轮通过 WPS 后台渲染四份文档，再用 Poppler 生成每页 PNG：

| 文档 | 页数 |
|---|---:|
| Manuscript | 34 |
| Supplementary Information | 19 |
| Research Proposal | 6 |
| Cover Letter | 1 |
| 总计 | 60 |

生成记录绑定四份 Markdown SHA-256 与四份 DOCX SHA-256；渲染记录再次绑定
四份 DOCX 和四份 PDF 的大小与 SHA-256，防止“正文已改但沿用旧文档检查”。

完成全 60 页缩略总览检查，并对投稿信、补充表 S8/S9、主文声明及附件说明等
关键页面放大核查。最终主文第 25-26 页确认：待再次批准、旧 DOI 范围、AI 使用责任
及新统计附件说明确实出现在当前 PDF 中，未出现相互遮挡或页面越界。
补充表 S8 列宽经生成器调整后重新渲染，不是手改输出文档。

自动检查结果：60 页文本均在画布内、没有未解析插图标记，补充 DOCX 嵌入
10 张补图；四份 DOCX 可访问性审计的高/中/低级问题计数均为 0。
缩略检查和自动规则不能替代作者逐句审阅或外部专业排版验收。
本机没有 LibreOffice；没有宣称完成 WPS 与 LibreOffice 的交叉渲染验证。

## 8. 回归测试和移植测试

| 检查 | 结果 |
|---|---|
| 科学门控及归一化回归 | 9/9 |
| 文档生成回归 | 3/3 |
| 新审阅包回归 | 7/7 |
| audit_tools 递归 PowerShell 语法解析 | 37 文件，0 错误 |
| 当前包标准库验证器 | PASS |
| 改变展开目录后标准库验证 | PASS |
| Git 已提交对象与冻结来源的字节比较 | 62/62 |
| Git 差异空白检查 | 通过 |

新包测试覆盖：大小/哈希篡改、缺失和额外文件、路径越界、确定性 ZIP、
授权状态不得擅自升级、旧写入入口禁用、Markdown 修改后不得沿用旧 DOCX 通过记录。

可移植性测试将最终 ZIP 展开到名称包含空格的新目录，再运行：

```powershell
python -I -S verify_review_bundle.py --bundle .
```

`-I -S` 排除用户 site-packages 和 Python 环境路径影响；验证器仅使用标准库，
不访问原分析目录。**这是换目录的离线验包测试，不是干净机器上重新运行完整科学模型。**
要重新计算仍需下载原始公共数据和建立科学环境；现有环境锁不能被说成已完成
本轮所有数值分析与 WPS 发布工程的跨机器重建验收。

一次包测试最初在 `D:\bioinfor\python.exe` 失败，原因是测试导入的历史文档模块
需要 `python-docx`，而该解释器未安装。改用已配置的文档 Python 后 7 项全部通过。
科学测试使用 `sle-bcell`；文档/包回归使用 bundled Python。未将环境缺包当作数据问题。

## 9. GitHub 同步与字节一致性修复

起始远端主分支：`b180b4743ff805c4f2f03a18b7f55f0a94162317`。

已同步的主体更正提交：`685fab6`。

字节保护修正提交及已确认的远端节点：
`1f1b5db6e72e6acba7b7d700dd5c45b871707ac1`。

同步地址：<https://github.com/1209433622cz-maker/sle-bcell-remodeling>。

同步前发现 Git 对一份 S9 图源 CSV 自动转换了 CRLF，造成原始字节哈希不匹配。
首次本地提交后仍未通过，因此没有把该状态推送出去。添加明确的生成物字节
保留属性、重新暂存原始文件后，以后续提交修复；没有修改任何数据值或冻结清单。
空白检查只对受保护文件接受 CR 行尾，仍检查真正的行尾空格等问题。

直接从已提交的 Git 对象读取并核验：45 份图件/图源、13 份精简校准有效载荷、
4 份受冻结输入清单约束的科学源码，共 62 项全部符合大小与 SHA-256。
机器可读证据为 `git_blob_verification.json`。这里的 13 项不含校准清单自身，
因此与新附件所述 14 份记录并不矛盾。

推送使用正常 fast-forward，没有 force、没有重写已发布历史。随后通过远端查询
确认 main 为上述节点。原始矩阵、逐细胞预测、WPS 中间目录、生成包和本地测试
副本没有加入 Git。本行动报告、核验回执和最终 checklist 以单独收尾提交保存；
不把报告所在提交的未知自引用哈希写入报告。

没有新建或修改 GitHub release，没有更新 Zenodo、移动旧标签或投稿。

## 10. 下一阶段的导师判断

**建议进入“外部方法复核与作者再次批准”，暂不追加阈值搜索、映射器替换或更多装饰性图件。**
当前能守住的是程序层 IFN 复现与透明的身份/组成边界，不能扩大为稳定细亚群、
绝对 B_ASC 扩增、源标签无关复现、IFN 重叠独立的 STAT2 机制或临床预测工具。
新增审阅包提高的是证据透明度与可核查性，不会自动提高生物学创新性。

| 优先级 | 下一任务 | 完成标准 |
|---|---|---|
| P0 | 外部方法审阅 | 由非本代理的审阅者检查归一化公式、门控撤回、候选族及推断边界，并留存具体意见 |
| P0 | 作者重新确认重大更正 | 两位作者审阅当前主文、补充材料、S10、投稿信及不确定性声明；留存针对当前包的明确确认 |
| P1 | 选刊和叙事定位 | 根据现有证据强度、公共数据再分析增量和期刊实际范围确定目标，不预设 Genome Medicine，也不承诺 Q1 接收 |
| P1 | 最终可复现归档 | 补齐需要的独立环境复现记录；批准后重新构建对应包、锁定 release commit，并发布匹配的新版本归档/DOI |
| P1 | 期刊特定最终检查 | 按目标期刊调整格式、声明、图宽和上传角色后重新验证，不能直接上传目前的 DRAFT map |
| 可选后续研究 | 更强外部证据 | 另立设计获取独立标注、治疗/纵向协变量或实验调控证据；不得追溯包装为原研究已预注册的验证 |

即使今后公开更正包，C9 校准 HOLD 也可作为透明限制保留；不要求为了投稿
一定把它调成 PASS。更重要的是读者能够区分原主分析仍支持什么、旧 C9 支持为何
失效、哪些结果没有计算，以及作者愿意承担什么范围的结论。

**本轮不需要用户再次下载大文件或运行耗时脚本。** 当前下一步的瓶颈是独立审查、
作者对重大更正的再次确认和选刊判断，不是计算尚未完成。

## 11. 交付物索引

- 本报告：`00_project_management/action_record_2026-08-28_correction_release_reconciliation.md`。
- 当前审阅包：`04_submission/correction_review.zip` 及同名展开目录。
- 当前 WPS 文档：`phase17_v7/post_gateC9/20260828_release_review/documents/`。
- 校准复核：`00_project_management/post_gateC9_release_review_2026-08-28/calibration_recount_audit.json`。
- 包构建与哈希：同目录 `correction_package_build.json`。
- 换目录标准库验包：同目录 `portable_relocation_stdout.json`。
- Git 对象核验：同目录 `git_blob_verification.json`。
- 全页渲染及最终文档哈希：同目录 `document_pages/document_render_audit.json`。
- 新构建和独立验包入口：`audit_tools/build_correction_review.ps1`、`audit_tools/verify_review_bundle.py`。

所有“完成”均限定在上述实际执行范围；未把自动检查代替作者批准、外部科学审查或期刊接收。
